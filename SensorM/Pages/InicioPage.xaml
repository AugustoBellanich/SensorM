<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SensorM.Pages.InicioPage"
             Title="Inicio">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Orientation="Horizontal" Padding="10">
                <Label Text="Modo oscuro" VerticalOptions="Center"/>
                <Switch IsToggled="{Binding IsDarkMode}" Toggled="OnThemeToggled" VerticalOptions="Center"/>
            </StackLayout>

            <Button Text="Buscar Dispositivos" 
                    Grid.Row="1" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding BuscarDispositivosCommand}"/>

            <CollectionView Grid.Row="2" 
                            ItemsSource="{Binding Dispositivos}" 
                            BackgroundColor="{DynamicResource CurrentBackgroundColor}" 
                            SelectionMode="Single" 
                            SelectionChanged="OnSelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10" 
                                     Margin="0,5" 
                                     Orientation="Horizontal" 
                                     BackgroundColor="{DynamicResource CardBackgroundColor}">
                            <Label Text="{Binding Nombre}" 
                                   Style="{StaticResource BaseLabelStyle}" 
                                   HorizontalOptions="StartAndExpand"/>
                            <Label Text="{Binding Tipo}" 
                                   Style="{StaticResource StatusLabelStyle}" 
                                   HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage>


